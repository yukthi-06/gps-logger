create an android application which has a screen with a two buttons labelled "Start Tracking" and "End Tracking". 
On button click of "Start Tracking", the app has to start logging the GPS co-ordinates and timestamp to a file in JSON format.
On button click of "Stopt Tracking", the app has to stop logging the GPS to the file .
Th json file has to be saved in user space directory 
It should start and stop GPS/location automatically or with prmission from user.
develop the app in java 
Give it as a single prompt to give to an agent to achieve this
store the timestamp as ISO format instead of milliseconds since epoc
--------------------------------------------------------------------------------------------
more prompts
*	When app is minimized, it should be floating on screen ie.. PIP mode (just like google maps does)
*	Have a status bar on top, just to show status as COLOR code... 
		GREEN = GPS locations is being received and logging to file
		BLUE  = GPS logging paused by user
		BROWN = Not receving GPS locations
		RED   = User has STOPPED logging.  Or User has not started logging.
	The same color, along with App Icon, should be the only thing shown when in PIP mode.
*	make the LAT and LON text smaller, so that it fits to single line. Make the GPS co-ordinates field accordingly smaller
*	Make the text for ACTIVITY view to be "Tracking... (? fixes)".. so that the whole text comes in one line.  This will stop the field to keep changing shapes because of 1 line and two lines....
*	Keep 'Tracking Info field' visible at all times, so that the buttons dont keep moving up and down

*	Save any MD file created by AI in the project folder itself.
*	the filename should be suffixed with *_yyyyMMdd.hhmmss
*	In the tracking info field, place a small "Tick" mark, so that once I type text in the field, and click on this "Tick" mark, the field is frozen and no longer editable
*	small correction.  Create a folder in the project folder 'Prompts' and create the MD files inside it.  Also, prefix with timestamp, instead of suffix
*	move the project settings and data folder base from /sdcard/Downloads/GPSLocationLogger to /sdcard/Vypeensoft/GPS_Location_Logger
*	On clicking "End tracking", app crashes.  
*	Also final loctaion to save GPX and other files is '/sdcard/Vypeensoft/GPS_Location_Logger'
*	on the left side menu, create a new menu item by name 'Tracks List'.  
	Clicking on this should launch a new screen which shows list of tracks from the saved folder.  
	Since for each track there are 3 files(gpx, kml, json), only one item should be shown in the tracks list.
*	'Tracks List' screen is empty.  It should pick up files from the data folder at /sdcard/Vypeensoft/GPS_Location_Logger/
*	It says "Folder is empty".  File read permission is not asked at start of app
*	Dont show the full filename in the list item text... extract the tracking_info  portion of the file name.. (ie.. skip the 'location_log', skip the time stamp). 
	If there is no user provided tracking info data, then show the timestamp as list item text.
	Along with list item name, show timestamp as a small font below it
*	Remove the millisecond from the display...
	If the Timestamp is the main text, then subtitle need not display the timestamp again.
*	Like the 'Tracks List' screen, have a left arrow to close screen on 'Settings', 'Help' and 'About' screen also.
*	Tracks list has to be sorted by timestamp in descending order.....(newest on one top)
*	On each track item, show small button icons for "Show on map", "Share", "Delete".  
*	on clikcing on 'map' button, it should open a new screen and load a map(from libre map library) and show the route on that map.  The map must be zoomable.
*	Use the below library instead of osmdroid

    // MapLibre SDK (Open Source fork of Mapbox)
    implementation "org.maplibre.gl:android-sdk:11.5.0"
    implementation "org.maplibre.gl:android-plugin-annotation-v9:3.0.1"

*	show on map from GPX, KML or JSON file.  Check for files in that order
*	download the relevant map tiles for offline use
*	show download status until download completes
*	the screen is stuck at 'calculating download size...'
*	
*	
*	
*	
*	
*	
*	
*	
*	
*	

--------------------------------------------------------------------------------------------

output

You are an expert Android developer. Build a complete Android application in **Java (not Kotlin)** using Android Studio.

## App Overview

Create a simple app with one screen containing two buttons:

* "Start Tracking"
* "End Tracking"

## Functional Requirements

### Start Tracking:

* When user clicks "Start Tracking":

  * Check for location permission (ACCESS_FINE_LOCATION)
  * If not granted, request it at runtime
  * Once granted:

    * Start GPS/location tracking using FusedLocationProviderClient
    * Capture:

      * Latitude
      * Longitude
      * Timestamp (ISO 8601 format)
    * Collect updates every 5–10 seconds
    * Store the collected data in memory as a JSON array

### End Tracking:

* When user clicks "End Tracking":

  * Stop location updates immediately
  * Save all collected data into a JSON file
  * File must be properly closed and flushed

## Timestamp Format

* Store timestamp in ISO 8601 format, e.g.:
  "2026-04-12T14:30:00Z"
* Use Java time APIs (java.time package), such as:

  * Instant.now()
  * DateTimeFormatter.ISO_INSTANT

## JSON Format

Store data like:
[
{
"latitude": 12.9716,
"longitude": 77.5946,
"timestamp": "2026-04-12T14:30:00Z"
}
]

## File Storage

* Save file in app-specific external storage:
  getExternalFilesDir(null)
* File name format:
  location_logs_<current_timestamp>.json
* Ensure compatibility with scoped storage (no MANAGE_EXTERNAL_STORAGE)
* Log the saved file path using Logcat

## UI Requirements

* Use ConstraintLayout
* Center two buttons vertically:

  * "Start Tracking"
  * "End Tracking"
* Initially:

  * "Start Tracking" enabled
  * "End Tracking" disabled
* While tracking:

  * Disable "Start Tracking"
  * Enable "End Tracking"

## Technical Requirements

### Location

* Use FusedLocationProviderClient (Google Play Services)
* Use LocationRequest with:

  * Priority: PRIORITY_HIGH_ACCURACY
  * Interval: ~5000–10000 ms
* Use LocationCallback to receive updates
* Properly stop updates using removeLocationUpdates()

### Permissions

* Handle runtime permission:
  ACCESS_FINE_LOCATION
* Handle:

  * Permission granted
  * Permission denied
  * "Don't ask again" scenario
* Show Toast messages for user feedback

### Lifecycle Handling

* Avoid memory leaks
* Stop location updates in onDestroy()
* Prevent multiple tracking sessions

## Error Handling

* Handle null locations
* Handle IO exceptions while writing file
* Log important events

## Project Structure

Provide complete code for:

1. MainActivity.java
2. activity_main.xml
3. AndroidManifest.xml
4. Gradle dependencies (Module-level)

## Dependencies

Include:

* Google Play Services Location library

## Code Quality

* Write clean, readable Java code
* Add comments explaining logic
* Use proper naming conventions
* Avoid deprecated APIs

## Optional (if possible)

* Show live coordinates on screen (TextView)
* Add logs for debugging

## Output Format

Return:

* Full working code for all files
* Clearly separated sections
* No pseudocode — only runnable code

Ensure the app runs on Android 8+ (API 26 and above).

