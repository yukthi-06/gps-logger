# User Prompt
My bad, the filename is indeed prefixed by gps_ folowed by timestamp(without mllisecond and timezone).
Bring the code to that state

## Objective
Refine the track loading logic in `TracksListActivity.java` to support track filenames prefixed with `gps_` (or `location_logs_`), followed by a 17-character timestamp in `YYYY-MM-DD.HHmmss` or `YYYY-MM-DDThhmmss` format (without milliseconds or timezone).

## Proposed Changes
1. **`TracksListActivity.java`**:
   - Update `parseTrackItemDetails(TrackItem item)` to parse baseName when prefixed with a timestamp of format `YYYY-MM-DD.HHmmss` or `YYYY-MM-DDThhmmss` (with dot or 'T' separator, 17 characters total), after removing the `gps_` or `location_logs_` prefix.
   - Extract the timestamp (first 17 characters of the stripped filename) and remainder (tracking info).
   - Update `formatTimestamp(String rawTimestamp)` to format `YYYY-MM-DD.HHmmss` and `YYYY-MM-DDThhmmss` as `YYYY-MM-DD hh:mm:ss` using colon separators for time.
   - Keep the original logic as a fallback for backward compatibility.

## Verification Plan
1. Check that filenames like `gps_2026-05-31.172348_MyTrip` are correctly split into:
   - Display Name: `My Trip`
   - Display Timestamp: `2026-05-31 17:23:48`
2. Check that filenames like `gps_2026-05-31.172348` are correctly split into:
   - Display Name: `2026-05-31 17:23:48`
   - Display Timestamp: `null`
