# User Prompt
remove label "label_save_path" at the bottom of the screen

## Objective
Remove the default static "Files saved to: Vypeensoft/GPS_Location_Logger/" label from the bottom of the main screen, while keeping the dynamic "Last saved: <filename>" indicator that appears after saving a track.

## Proposed Changes
1. **`activity_main.xml`**:
   - Change the `TextView` with ID `tvSavePath` to be hidden by default (`android:visibility="gone"`).
   - Remove the default text `@string/label_save_path` to avoid layout flashing or showing static path info.

## Verification Plan
1. Launch the app and confirm that the bottom of the screen is clean and does not show the "Files saved to: ..." label.
2. Record/save a track and confirm that the "Last saved: <filename>" indicator correctly appears at the bottom.
